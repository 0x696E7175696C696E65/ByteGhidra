{
  "name": "ghidra-mcp",
  "version": "0.1.0",
  "description": "stdio MCP wrapper for the Ghidra MCP bridge extension",
  "type": "commonjs",
  "bin": {
    "ghidra-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.js"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
