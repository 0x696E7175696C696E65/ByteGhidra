# Reference UI Client

This directory is reserved for a minimal validation client, not a product UI.

The implemented client contract currently lives in `re_platform::ui::ReferenceClient` and is exercised by integration tests. It supports opening a project summary, fetching CFG slices, and submitting user corrections through the backend service API.
